# フック

フック（Hooks）は、関数コンポーネントでstateやライフサイクル機能を利用するための特別な関数です。

### 主要なフック

- **`useState`**: 状態（state）を保持・管理する。
- **`useEffect`**: 副作用（副作用処理：API通信、DOM操作、タイマーなど）を実行する。

フックはコンポーネント間で状態管理ロジックを再利用可能にするための革命的な仕組みであり、Reactの現在の開発スタイルにおける基盤です。

