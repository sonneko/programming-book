# Reactの思想

Reactの最大の特徴は、UIを「状態（state）の関数」として捉える思想です。

### 宣言的UI

命令的に「DOMをこのように変える」という指示を出すのではなく、「この状態のときは、このUIであるべきだ」という状態（state）を宣言します。Reactがその状態の変化を検知し、最適なDOM操作を自動的に行ってくれます。

### データフロー

Reactにおけるデータは一方向（トップダウン）で流れます。親から子へpropsを通じてデータが伝播します。

このシンプルなモデルにより、アプリケーションの状態管理が容易になり、UIの挙動を予測しやすくなります。

