# propsとchildrenの扱い

Reactコンポーネントにおける `props` は、親コンポーネントから子コンポーネントへデータを渡すための手段です。

### propsの使用法

propsは関数の引数のように受け取ります。

```jsx
function Welcome(props) {
  return <h1>Hello, {props.name}</h1>;
}
```

### childrenプロパティ

`children` は特殊なpropsで、コンポーネントの開始タグと終了タグの間に囲まれた要素を指します。

```jsx
function Wrapper({ children }) {
  return <div className="wrapper">{children}</div>;
}

<Wrapper>
  <p>囲まれた要素</p>
</Wrapper>
```

これらを活用することで、コンポーネントの構成を柔軟にし、再利用性の高いUI部品を構築できます。

