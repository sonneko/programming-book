# SQL文3

複数のテーブルを組み合わせてデータを取得する「結合 (JOIN)」は、リレーショナルデータベースの真骨頂です。

### テーブル結合

```sql
SELECT users.name, posts.title
FROM users
JOIN posts ON users.id = posts.user_id;
```

`JOIN` を使うことで、正規化されたテーブル同士を関連付け、複雑なデータセットを一つの結果として取得できます。

