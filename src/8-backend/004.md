# JWTによる認証

JWT (JSON Web Token) は、サーバーとクライアント間で安全に情報をやり取りするための認証トークン形式です。

### 特徴

- ステートレス: サーバー側でセッション情報を保持する必要がないため、スケーラビリティが高い。
- 自己完結型: トークン自体にユーザーID等の情報が含まれている。

クライアントがログイン時にJWTを受け取り、以降のリクエストのヘッダーに含めることで、認証状態をサーバー側に伝えます。

