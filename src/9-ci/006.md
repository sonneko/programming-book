# Gitコマンドチートシート

覚えておくと便利なGitコマンド集。

- `git status`: 現在の状態確認（頻繁に使う）。
- `git diff`: 変更内容の確認。
- `git branch`: ブランチ一覧。
- `git checkout -b <name>`: 新しいブランチを作成して移動。
- `git pull`: リモートの変更をローカルに取り込む。
- `git stash`: 変更を一時退避させる。

これらを手に馴染ませることで、開発中のGit操作におけるストレスが激減します。

