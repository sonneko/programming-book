# Node.js導入

TypeScriptを扱う前段階として、JavaScriptの実行環境であるNode.jsの導入が不可欠です。Node.jsは、ブラウザ以外でJavaScriptを実行可能にするランタイムです。

### Node.jsの役割

Node.jsは、Google ChromeのV8エンジンをベースにしており、高いパフォーマンスを提供します。Webサーバーの開発だけでなく、フロントエンド開発ツールの実行基盤としても広く使われています。

### 導入方法

公式のインストール方法として、`nvm`（Node Version Manager）を利用することを強く推奨します。

```bash
# nvmを使ったインストール例
nvm install --lts
nvm use --lts
```

これにより、複数のNode.jsバージョンを簡単に切り替えることができ、プロジェクトごとに異なる環境を管理することが可能になります。

