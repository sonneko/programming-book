# 型注釈

TypeScriptにおける「型注釈（Type Annotation）」は、変数、引数、戻り値に型を明示的に付与する機能です。これにより、コードの意図が明確になり、コンパイラによる自動チェックが可能になります。

### 基本的な型注釈

```typescript
let username: string = 'Alice';
let age: number = 30;
let isActive: boolean = true;
```

### 関数の型注釈

関数の引数と戻り値に型を指定することで、呼び出し元と実装の整合性が担保されます。

```typescript
function greet(name: string): string {
  return `Hello, ${name}`;
}
```

型注釈を適切に行うことで、IDEの補完機能が強化され、開発スピードと品質が両立されます。TypeScriptは「ドキュメントとしてのコード」を実現する優れた言語です。

