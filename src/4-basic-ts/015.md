# 非同期関数

`async` 関数と `await` キーワードを使用することで、非同期処理を非常に直感的に記述できます。

### async/awaitの利用

```typescript
async function fetchData(): Promise<string> {
  const response = await fetch('https://api.example.com/data');
  const data = await response.json();
  return data;
}
```

- `async` キーワード: 関数が常に `Promise` を返すようにします。
- `await` キーワード: `Promise` が解決（成功）されるまで関数の実行を一時停止します。

`try-catch` ブロックを使って同期処理と同じようにエラーハンドリングができるため、コードの可読性が大幅に向上します。

