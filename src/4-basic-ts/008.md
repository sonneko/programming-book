# インターフェース

インターフェース（`interface`）は、オブジェクトが持つべきプロパティやメソッドの構造を定義する契約です。

### インターフェースの定義と実装

```typescript
interface User {
  name: string;
  age: number;
}

const alice: User = {
  name: 'Alice',
  age: 25
};
```

インターフェースはコンパイル後のJavaScriptには残らないため、パフォーマンスへの影響はありません。複数のクラスや関数で共通のデータ構造を定義する場合、クラスを継承するよりもインターフェースで型を定義する方が、疎結合な設計を維持できます。

