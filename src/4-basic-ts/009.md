# 抽象クラス・継承

抽象クラス（`abstract class`）は、インスタンス化できないクラスで、派生クラスに実装を強制する基盤として利用します。

### 抽象クラスの役割

```typescript
abstract class Animal {
  abstract makeSound(): void;
  
  move(): void {
    console.log('Moving...');
  }
}

class Dog extends Animal {
  makeSound() {
    console.log('Woof!');
  }
}
```

抽象クラスは「is-a」関係を定義するのに適しています。共通の機能は抽象クラスで実装し、個別の機能は派生クラスで実装することで、コードの重複を防ぎ、設計の統一感を保てます。

