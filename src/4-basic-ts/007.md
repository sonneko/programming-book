# 静的フィールド・メソッド

クラスのインスタンスではなく、クラス自体に紐付くフィールドやメソッドを定義するには `static` キーワードを使用します。

### 静的メンバーの活用

```typescript
class Config {
  static readonly API_URL = 'https://api.example.com';

  static getBaseUrl(): string {
    return this.API_URL;
  }
}
```

インスタンスを作成せずに、直接 `Config.API_URL` や `Config.getBaseUrl()` として呼び出せます。これは、設定値の保持やユーティリティ関数のような、状態を保持しない機能の集約によく用いられます。

