# 非同期処理

JavaScriptはシングルスレッドで動作するため、時間のかかる処理（ネットワーク通信やファイルI/O）を同期的に行うと、画面がフリーズしてしまいます。これを解決するのが「非同期処理」です。

### 非同期処理の進化

1. **コールバック関数**: 古い手法。ネストが深くなりやすく（コールバック地獄）、エラーハンドリングが困難でした。
2. **Promise**: 非同期処理の状態（成功・失敗）をオブジェクトとして表現し、チェーンすることで解決します。
3. **Async/Await**: Promiseをより同期的なコードのように記述できる現代的な構文。

非同期処理を正しく理解することは、特にフロントエンドにおけるAPI通信や、バックエンドにおけるDB操作において不可欠です。

