# クラスの表記法

TypeScriptでは、モダンなJavaScriptのクラス構文をベースに、型定義を加えた強力なクラスを記述できます。

### クラスの基本構造

```typescript
class User {
  name: string;
  private age: number;

  constructor(name: string, age: number) {
    this.name = name;
    this.age = age;
  }

  getAge(): number {
    return this.age;
  }
}
```

### アクセス修飾子

- `public`（デフォルト）: どこからでもアクセス可能。
- `private`: クラス内からのみアクセス可能。
- `protected`: クラス内および派生クラスからのみアクセス可能。

これらを利用してデータのカプセル化を行い、外部から直接的なデータ操作を防ぐことで、クラスの設計意図を維持します。

