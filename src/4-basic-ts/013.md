# ESmoduleとcommonJS

JavaScriptには、コードをモジュールとして分割し、再利用するための仕組みが二つ存在します。

### モジュールシステムの変遷

- **CommonJS**: Node.jsが初期から採用しているモジュールシステム。`require` と `module.exports` を使用します。
- **ES Modules (ESM)**: ECMAScriptで標準化された現代的なモジュールシステム。`import` と `export` を使用します。

### どちらを使うべきか

現代のTypeScript開発では、**ES Modules (ESM)** の使用を強く推奨します。

```typescript
// エクスポート
export const add = (a: number, b: number) => a + b;

// インポート
import { add } from './math';
```

ESMはブラウザとNode.jsの両方で利用可能で、静的な解析が容易であるため、バンドラーやTypeScriptによる最適化が効きやすいというメリットがあります。

